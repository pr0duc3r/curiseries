import { PageHeader } from 'antd';

const HeaderTitle = () => (
    <PageHeader
        className="site-page-header"
        onBack={() => null}
        title="Curitx Recomienda"
        subTitle="El oráculo de Curi pero de series."
    />
  );
  
  export default HeaderTitle;
  